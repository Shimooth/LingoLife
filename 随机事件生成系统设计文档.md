# 生活模拟与涌现剧情系统设计文档

> 文件名为兼容既有引用暂时保留为“随机事件生成系统设计文档.md”。本文已不再采用“每日随机派发任务”的架构。

- 版本：0.3
- 项目：LingoLife
- 上位设计：[`LingoLife GDD.md`](LingoLife%20GDD.md)
- Agent 契约：[`NPC Agent 系统设计文档.md`](NPC%20Agent%20系统设计文档.md)
- 实施计划：[`docs/LIFE_SIMULATION_IMPLEMENTATION_PLAN.md`](docs/LIFE_SIMULATION_IMPLEMENTATION_PLAN.md)

---

# 一、系统目标

本系统负责把 NPC 的持续生活转换为可观察、可延续的内容。

系统不再从“今天需要几个事件”出发，而从以下事实出发：

- NPC 当前在做什么；
- NPC 原本想做什么；
- 谁和谁在同一地点；
- 哪些资源被占用；
- 关系中有哪些吸引、信任、积怨和秘密；
- Household 存在哪些责任、边界和稀缺资源；
- 过去有哪些未解决问题；
- 玩家最近改变了哪些条件。

事件不是生活的起点，而是规则层对行为碰撞的分级结果。

---

# 二、内容层级

## 2.1 Ambient Action

单个居民执行的普通生活行为，例如吃饭、洗澡、睡觉、阅读、练琴和发呆。

特征：

- 高频；
- 通常没有 UI 弹窗；
- 不要求关系、世界或英语数值变化；
- 通过位置、动作和简短标签可观察；
- 可以成为后续碰撞的输入。

## 2.2 Moment

短暂、有性格但通常不需要解决的生活片段，例如：

- 两人一起看雨；
- 一人给另一个人留了早餐；
- 两人想看不同的电视节目；
- 室友路过时顺手收拾了杯子；
- 一个人想加入谈话又退缩了。

Moment 可以只产生表现和轻量记忆，不强制产生奖励。

## 2.3 Incident

带有明确需要、冲突、风险或关系转折的情况，例如：

- 家务不平衡累积到一方提出不满；
- 一名居民误用他人的私人物品；
- 两名居民争吵；
- 一名居民准备告白；
- 重要约定被忘记；
- 长期疲惫影响工作和相处。

Incident 可以提供有限观察或干预窗口，但仍能自主处理。

## 2.4 Story Thread

跨多个行为、Incident 或日期延续的故事线，例如：

- 室友因家务形成积怨，争吵后重新分工；
- 单向心动逐渐变成暧昧、告白或放下；
- 一个人总是照顾他人，最终感到疲惫；
- 长期目标受到朋友支持、嫉妒或现实条件影响。

Story Thread 不等于固定任务链。每个阶段根据实时状态重新评估，允许停滞、偏转和自然结束。

---

# 三、生成总流程

```text
推进时间、需求和环境
↓
为 NPC 生成 Desire 候选
↓
选择 Commitment 并启动 Life Action
↓
检测人物、资源、地点和关系碰撞
↓
生成客观事实与参与角色
↓
按影响分级为 Ambient / Moment / Incident
↓
连接或创建 Story Thread / Unresolved Thread
↓
计算可观察性和干预窗口
↓
自主处理或接受玩家输入
↓
事务化写入结果、主观记忆和余波
```

任何阶段都不允许 LLM 创建参与者、地点、资源占用或结果。

---

# 四、行为碰撞

## 4.1 人与资源

当两个 Commitment 指向同一个容量不足的资源时产生碰撞，例如电视、浴室、灶台和唯一一份食物。

可能处理：等待、加入、协商、让步、争抢、改用替代资源、离开或求助。

## 4.2 人与人

主动交谈、邀请、照顾、回避、告白、道歉和请求帮助需要目标 NPC 当时可用，并允许对方接受、延迟或拒绝。

## 4.3 人与责任

家务、承诺和共同计划产生责任碰撞。系统记录谁制造工作、谁认为该由谁处理、谁实际承担及是否被看见。

## 4.4 人与边界

使用私人空间、借用物品、谈论秘密、身体距离和关系撮合可能触碰边界。边界冲突不能只按好感判断。

## 4.5 人与环境

天气、地点关闭、物品损坏、库存不足和城市设施改变可以阻碍 Commitment，并促使替代、合作或情绪变化。

---

# 五、规则模板

## 5.1 模板职责

模板描述碰撞规则和允许结果，不预写唯一剧本。

```json
{
  "id": "shared_resource_conflict",
  "level": "moment_or_incident",
  "trigger": "resource_capacity_exceeded",
  "roles": ["current_user", "waiting_user"],
  "preconditions": {
    "same_household": true,
    "resource_kind": ["television", "bathroom", "kitchen"]
  },
  "responses": [
    "yield",
    "wait",
    "negotiate",
    "join",
    "argue",
    "silently_leave",
    "ask_third_person"
  ],
  "thread_hook": "resource_boundary"
}
```

## 5.2 模板输入

- 参与者 Persona、Need、Desire 和 Commitment；
- 双向关系和相关第三人；
- Household、资源和地点事实；
- 近期共同记忆；
- 匹配的 Unresolved Thread；
- 当前时间、日程和压力；
- 世界种子和规则版本。

## 5.3 模板输出

- 客观事实；
- 内容层级；
- 参与者角色；
- 允许响应和结果集合；
- 可观察表现；
- Trouble Signal 资格；
- 关系和需求变化范围；
- 记忆视角标签；
- Thread 创建、推进或关闭操作；
- 允许的动画与地点要求。

模板不能直接包含 LLM 自由生成的数值。

---

# 六、首轮内容库

## 6.1 个人日常行为

首轮包含：做饭、吃饭、睡觉、洗澡、看电视、阅读、投入兴趣或个人目标、向朋友借用物品、打扫、留下脏盘子、独处、寻找陪伴和主动聊天。

## 6.2 室友微摩擦

优先实现八类：

1. 谁洗碗；
2. 谁倒垃圾；
3. 谁拿遥控器；
4. 偷吃或误吃别人的食物；
5. 半夜制造噪声；
6. 借东西没有归还；
7. 占用浴室太久；
8. 一个人长期承担照顾或清洁。

每类至少支持三种由人格和关系决定的反应，不能只有“争吵/不争吵”二元结果。

## 6.3 友好 Moment

首轮至少包含：分享食物、共同娱乐、顺手帮助、安静陪伴。友好行为也需要目标 NPC 的接受和当时状态。

## 6.4 关系转折

恋爱、告白和分手属于核心社会模拟。方向性 attraction 与年龄、亲属、个人边界、共同经历从关系基础阶段纳入数据契约；单向心动不写入对方状态，约会和伴侣必须由明确接受事件建立。具体告白、拒绝、约会、分手和前任内容在 Life Action、资源碰撞与关系证据稳定后接入，不能把友情做成恋爱的自动升级树。

---

# 七、分级与提升

## 7.1 Moment 评分

```text
moment_score =
  novelty
  + personality_expression
  + relationship_relevance
  + visual_readability
  + player_context_relevance
  - recent_repetition
```

低分碰撞可以静默完成；高分碰撞进入可观察 Moment。

## 7.2 Incident 评分

```text
incident_score =
  need_stakes
  + conflict_intensity
  + relationship_transition_risk
  + unresolved_thread_pressure
  + goal_impact
  + household_impact
  - safe_autonomous_capacity
```

达到阈值后提升为 Incident。高影响、不可逆结果必须提供有限窗口；普通争执和日常选择可以自主解决。

## 7.3 不使用固定事件配额

删除“每天必须生成 3～5 个主要事件和 5～10 个轻量事件”的要求。

改为密度预算：

- 行为持续发生；
- Moment 受 UI 可读性和重复度限制；
- Incident 可以某天为零，也可以在条件成熟时连续出现；
- 不为了满足数量强迫不相关角色相遇；
- 同时活跃 Story Thread 数量设上限，避免剧情噪声。

首轮建议每个 Household 同时最多 1 个开放 Incident、2 个活跃 Unresolved Thread，数值后续通过测试调整。

---

# 八、可观察性与烦恼气泡

## 8.1 现场表现

Ambient 和 Moment 优先通过动作、位置、资源状态和简短标签呈现，不进入统一事件列表。

玩家可以看到：

- 居民正在做什么；
- 两人是否正在互动；
- 资源是否被占用或留下痕迹；
- 明显的犹豫、回避、愤怒或疲惫；
- 允许公开的 Story Thread 摘要。

## 8.2 Trouble Signal

烦恼气泡只在以下条件成立时出现：

- NPC 愿意向玩家披露；
- 问题仍可影响；
- 玩家信任和关系边界允许；
- 当前没有更自然的 NPC 求助对象，或玩家确实是合适对象；
- UI 注意力预算允许。

气泡不是任务清单，也不保证玩家能够解决问题。

## 8.3 隐藏和事后发现

NPC 可以隐瞒、自行解决、只告诉其他居民或在事后提起。未观察内容通过房间痕迹、情绪、传闻、记忆和关系状态被间接发现。

---

# 九、玩家干预

## 9.1 动作生成

管理动作来自 Incident 上下文，不固定使用同一组按钮。可用类型包括：

- ask；
- comfort；
- advise；
- provide_resource；
- mediate；
- encourage；
- give_space；
- matchmake；
- let_them_handle_it。

只有符合事实和玩家身份的动作才能出现。

## 9.2 结果计算

管理动作先计算每名参与者的独立反应，再计算共同结果。

```text
participant_response =
  action_fit
  + trust_in_player
  + openness
  + current_need
  - pride
  - stress
  - boundary_violation
  + stable_seed_variation
```

允许立即接受、延迟接受、礼貌拒绝、误解、反效果和多人混合反应。`mediate` 不再固定增加信任、减少 tension。

## 9.3 英语交互

需要解释观点、安慰、建议和调解时进入英语交互。玩家表达产生语义信号，但语法完美度不直接决定角色是否接受。

---

# 十、自主与离线推进

## 10.1 自主结算

NPC 不会为了玩家观看而整天站在目的地等待。Moment 到达现场后直接表演并按规则完成；客户端可以回放摘要，但观看不是结算前置条件。

Incident 可以进入短暂 `intervention_window`。窗口结束后，根据参与者倾向自主处理。

## 10.2 离线模拟

离线期间按时间段批量推进：

1. 推进固定日程和需求；
2. 选择关键 Commitment；
3. 只模拟可能改变持久状态的碰撞；
4. 结算资源、关系、记忆和 Thread；
5. 生成有限数量的可回顾摘要。

不能逐秒回放，也不能把所有离线小事存入数据库。

## 10.3 余波

自主结果至少在需要时留下一个后续入口：主观记忆、环境痕迹、情绪变化、关系变化、传闻、计划改变或 Unresolved Thread。

---

# 十一、记忆与连续故事

## 11.1 客观事实与主观视角

系统先保存参与者、地点、资源、动作和客观结果，再为每名 NPC 写入不同视角。玩家介入作为事实记录，但不能让两人的解释自动一致。

## 11.2 Unresolved Thread

重复碰撞优先连接已有 Thread，而不是创建重复 Incident。例如第二次留下脏盘子会增加原有 `dishwashing` Thread 的次数和压力。

Thread 的解决需要主题匹配的证据：一次普通闲聊不能消除家务积怨；主动分担、明确道歉或重新分工可以。

## 11.3 Story Thread

Story Thread 保存当前阶段、相关 Thread、参与者、已发生事实、可用转折和冷却。下一阶段由当时世界重新判断，不保证沿预写路径前进。

---

# 十二、重复控制与稳定性

- 世界种子由玩家和世界版本确定；
- 行为、碰撞、Incident 和管理动作拥有稳定 ID；
- 同一事实重复读取不会重新抽取；
- 重复请求不会重复写入数值和记忆；
- 模板有冷却和近期模式惩罚；
- 相同主题重复时优先升级已有 Thread；
- 数据库迁移必须增量执行并保留旧事件历史。

---

# 十三、前端呈现原则

- 城市视图首先显示具体行为，而非统一 `idle`；
- Ambient Action 不弹窗；
- Moment 可以现场观看或事后看到简短摘要；
- Incident 通过现场状态和可选 Trouble Signal 暴露；
- Story 面板显示连续关系和余波，不做待办清单；
- 管理按钮不标记正确答案；
- 结果通过动作、台词和生活变化解释，不只显示 `relationship +N`；
- 玩家跟随只改变镜头，不改变 NPC 路线和行为。

---

# 十四、内容与成本边界

- 生活行为、碰撞、评分和数值完全规则化；
- Ambient Action 不调用 LLM；
- Moment 默认使用模板台词，仅在玩家主动观看且需要高质量表达时可调用模型；
- Incident 的事实和结果先结算，再按需调用 LLM 表达；
- 相同可见内容允许缓存和复用；
- DeepSeek 不可用时所有状态机与模板表达仍可运行。

---

# 十五、验收标准

- 四名居民在共享住宅中能够持续执行个人行为；
- 至少三类共享资源能产生等待、让步、协商和冲突；
- 同一模板因人格与关系产生至少三种反应；
- 大部分生活行为不进入事件列表；
- Incident 某天可以为零，系统不强行补足数量；
- Trouble Signal 只对愿意披露的 NPC 出现；
- 玩家选择相同动作不会跨情境固定得到正向结果；
- 未观察 Incident 能自主结算并留下次日余波；
- 重复家务问题能连接同一个 Unresolved Thread；
- 页面刷新、重复请求和服务重启不改变既有结果；
- 普通生活模拟不依赖 LLM。

满足以上条件后，再扩展三角关系与更复杂群体关系、八人 Household、婚姻与代际、宠物、服装和城市创作系统。单向心动、告白、拒绝、约会、分手和前任已属于本轮核心范围。
