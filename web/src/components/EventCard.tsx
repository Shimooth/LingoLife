import {AnimatePresence,motion,useReducedMotion} from 'motion/react'
import type {ActiveEvent,EventUpdate} from '../types'
export function EventCard({event,update}:{event:ActiveEvent|null;update:EventUpdate|null}){const reduce=useReducedMotion();return <AnimatePresence mode="wait">{event?<motion.aside className={`event-card category-${event.category}`} key={`${event.id}-${event.stage_index}`} initial={reduce?false:{opacity:0,y:-8}} animate={{opacity:1,y:0}} exit={{opacity:0}}><div className="event-head"><span>Today’s story</span><small>{event.category} · {event.stage_index+1}/{event.stage_count}</small></div><div className="event-progress" aria-label={`Event stage ${event.stage_index+1} of ${event.stage_count}`}><i style={{width:`${(event.stage_index+1)/event.stage_count*100}%`}}/></div><h2>{event.title}</h2><p>{event.stage.prompt}</p><strong>◆ Your moment</strong><small>{event.stage.objective}</small>{event.learning_targets.length>0&&<div className="event-targets">{event.learning_targets.map(x=><span key={x}>{x.replaceAll('_',' ')}</span>)}</div>}</motion.aside>:update?.completed?<motion.aside className="event-card event-complete" initial={reduce?false:{opacity:0,scale:.96}} animate={{opacity:1,scale:1}}><div className="event-head"><span>Story complete</span><small>Memory made ✦</small></div><h2>A moment to remember</h2><p>{update.memory||update.outcome?.memory||'Your response changed how this story ended.'}</p></motion.aside>:null}</AnimatePresence>}
